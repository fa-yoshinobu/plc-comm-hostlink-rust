# Release Publication - 2026-06-12

Package:

- repository: `plc-comm-hostlink-rust`
- tag: `v0.1.4`
- crate: `plc-comm-hostlink-rust`
- registry: crates.io
- GitHub Release: https://github.com/fa-yoshinobu/plc-comm-hostlink-rust/releases/tag/v0.1.4

Publication status:

- GitHub Release created.
- crates.io publish completed.
- `cargo search` confirmation completed.

Distribution audit:

- crate keeps `src/`, `docs/`, `examples/`, and `tests/` for reproducibility.
- README image references must use a repository/hosted URL, not a Windows
  absolute path.

Release process note:

- Before future publication, confirm that the crate version does not already
  exist in crates.io.
- Version number gaps are acceptable; duplicate publication of the same version
  is not acceptable.
